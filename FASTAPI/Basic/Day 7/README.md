# Day 7 — Blog Comments API

A small blog backend with posts and nested comments to show resource relationships.

Project files live under `app/` with `main.py`, `routers.py`, `schemas.py`, and `services.py`.
