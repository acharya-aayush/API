# Day 1 — Workout Tracker API

A lightweight API for logging workouts, listing sessions, and keeping service logic separate.

Project files live under `app/` with `main.py`, `routers.py`, `schemas.py`, and `services.py`.
