# Day 4 — Auth Simulation API

A small token-based auth demo with login and a protected profile endpoint.

Project files live under `app/` with `main.py`, `routers.py`, `schemas.py`, and `services.py`.
