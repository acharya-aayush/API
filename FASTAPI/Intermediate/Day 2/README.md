# Day 2 — Contact Directory API

Manage contacts with search, company association, and simple filtering.

Project files live under `app/` with `main.py`, `routers.py`, `schemas.py`, and `services.py`.
