# Day 7 — Service Request API

A service request API with patch updates, status transitions, and lifecycle state.

Project files live under `app/` with `main.py`, `routers.py`, `schemas.py`, and `services.py`.
