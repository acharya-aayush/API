# Day 4 — Event Scheduling API

Schedule events with date validation, update flows, and event retrieval.

Project files live under `app/` with `main.py`, `routers.py`, `schemas.py`, and `services.py`.
