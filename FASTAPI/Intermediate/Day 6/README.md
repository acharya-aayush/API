# Day 6 — Team Notes API

A user-specific notes API with header-based identity and personal note lists.

Project files live under `app/` with `main.py`, `routers.py`, `schemas.py`, and `services.py`.
