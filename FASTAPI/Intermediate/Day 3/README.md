# Day 3 — Inventory Status API

Manage inventory items and status with strict enums and response models.

Project files live under `app/` with `main.py`, `routers.py`, `schemas.py`, and `services.py`.
